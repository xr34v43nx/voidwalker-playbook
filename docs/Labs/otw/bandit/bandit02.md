# Goal
	Read a file named `-` in the home directory.

## What to learn
	 How to bypass bash filtering to prevent the shell from reading the file as a flag.


### Syntax of note:
- `./` to escape syntax trapping