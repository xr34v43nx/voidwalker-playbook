# Goal
	Find a hidden file within a directory.

## What to learn
	How to display hidden files.

### Commands of note
- `ls -la` - the flags following the command issue specific tasks for the utility to run.
- `-l` - prints the contents in long listing format
- `-a` - prints all contents